[build]
builder = "NIXPACKS"

[env]
JAVA_HOME = "/usr/lib/jvm/java-21-openjdk"
JAVA_VERSION = "21"

[phases.build]
cmds = [
    "chmod +x mvnw",
    "./mvnw clean package -DskipTests"
]

[start]
cmd = "java -jar target/*.jar"

